# Event Logs

This folder simulates raw JSON logs emitted by the application.
Each file represents a single event, similar to what would be
received from a streaming platform (e.g. Kafka / Kinesis).

Event types included:
- user_login
- document_edit
- comment_added
- document_shared